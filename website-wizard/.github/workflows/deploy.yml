name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to Server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 34.27.91.3
          username: bangura8591
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ~/website-wizard
            git pull
            docker compose down
            docker compose up -d --build